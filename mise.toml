[tools]
# 3.1.3 is the default (matches Gemfile.lock/gemspec). The bare specs are
# for the CI compatibility matrix (test.yml), which pins ruby via
# MISE_RUBY_VERSION per job -- each needs its own mise.lock entry to
# resolve under --locked mode.
ruby = ["3.1.3", "3.2", "3.1", "3.0", "2.7"]
